all:
  children:
    core_ipvpn:
      children:
        cisco_ios:
          hosts:
            csr1:
              ansible_host: 192.168.12.51
          vars:
            ansible_network_os: ios
  vars:
    ansible_connection: network_cli
